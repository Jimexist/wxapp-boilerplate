@import '../../styles/share.wxss';

:root {
	--r: 120rpx;
}

.container {
	@apply --center-flex;
	flex-direction: column;
	align-items: center;
}

.avatar {
	width: var(--r);
	height: var(--r);
	margin: 20rpx;
	border-radius: 50%;
}

.nickname {
	color: #aaa;
}
