@import '../../styles/share.wxss';

:root {
	--r: 24px;
}

.motto {
	@apply --center-flex;
	margin-top: 200px;
	align-items: center;
}

.face {
	width: var(--r);
	height: var(--r);
	border-radius: 50%;
	margin-right: 10px;
}

.powered {
	margin-top: 30px;
	font-size: 11px;
	color: gray;
}
